# Frozen market-data fixtures

`yfinance_returns_sample.csv` is a frozen public-market return slice written by the
first successful `python scripts/run_backtest.py` week-0 run. Blocking tests use
fixtures or synthetic data and never fetch the network.
